# Provenance

Each curated code/markdown cell includes `metadata.provenance.source_notebook` and `metadata.provenance.source_cell`. The source directory was read only. Embedded image outputs were retained except where a cell was deliberately replaced by centralized setup code.
