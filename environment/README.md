# Environments

The package lists document direct notebook dependencies. Record exact versions with `sessionInfo()` (R) and `python -m pip freeze` on the compute environment used for the final rerun.
